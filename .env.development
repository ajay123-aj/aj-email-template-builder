# Used when running `npm run dev` (Vite development mode)
# API base: http://localhost:3001/api-backend → lead collect → .../api-backend/api/aj-email-editor/leads/collect
VITE_LEAD_COLLECT_API_ORIGIN=http://localhost:3001
VITE_API_PREFIX=/api-backend
